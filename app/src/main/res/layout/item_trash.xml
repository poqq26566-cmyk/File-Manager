<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:paddingStart="@dimen/activity_margin"
    android:paddingTop="@dimen/normal_margin"
    android:paddingEnd="@dimen/small_margin"
    android:paddingBottom="@dimen/normal_margin">

    <com.goodwy.commons.views.MySquareImageView
        android:id="@+id/trash_item_icon"
        android:layout_width="42dp"
        android:layout_height="42dp"
        android:layout_centerVertical="true"
        android:layout_alignParentStart="true"
        android:padding="6dp"
        android:src="@drawable/ic_file_vector" />

    <LinearLayout
        android:id="@+id/trash_item_text_holder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toStartOf="@+id/trash_item_buttons"
        android:layout_toEndOf="@+id/trash_item_icon"
        android:layout_marginStart="@dimen/normal_margin"
        android:orientation="vertical">

        <com.goodwy.commons.views.MyTextView
            android:id="@+id/trash_item_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="middle"
            android:maxLines="1"
            android:textSize="@dimen/bigger_text_size"
            tools:text="document.pdf" />

        <com.goodwy.commons.views.MyTextView
            android:id="@+id/trash_item_subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:alpha="0.6"
            android:ellipsize="middle"
            android:maxLines="1"
            android:textSize="@dimen/smaller_text_size"
            tools:text="2.3 MB · Deleted 2026-07-25" />
    </LinearLayout>

    <LinearLayout
        android:id="@+id/trash_item_buttons"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:orientation="horizontal">

        <com.goodwy.commons.views.MySquareImageView
            android:id="@+id/trash_item_restore"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:padding="8dp"
            android:src="@drawable/ic_undo_vector" />

        <com.goodwy.commons.views.MySquareImageView
            android:id="@+id/trash_item_delete"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:padding="8dp"
            android:src="@drawable/ic_delete_outline" />
    </LinearLayout>
</RelativeLayout>
